# Web server — the log-set API and the Slack interactivity endpoint
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Slack Web API transport
httpx>=0.27.0

# Claude — the narration and resolution-judgement layer (agent/llm.py).
# Optional at runtime: with no ANTHROPIC_API_KEY, or with the package
# absent, every call falls back to deterministic text and says so.
anthropic>=1.7.0

# Config and contracts
PyYAML>=6.0.1          # config/severity.yml
jsonschema>=4.22.0     # schemas/incident.schema.json

# Testing
pytest>=8.0.0
